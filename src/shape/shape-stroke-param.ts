export enum LineCap {
  /** 默认值。向线条的每个末端添加平直的边缘 */
  Butt,
  /** 向线条的每个末端添加圆形线帽 */
  Round,
  /** 向线条的每个末端添加正方形线帽 */
  Square
}

export enum LineJoin {
  /** 创建圆角 */
  Round,
  /** 创建斜角 */
  Bevel,
  /** 创建尖角 */
  Miter
}
